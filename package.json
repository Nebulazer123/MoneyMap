{
  "name": "moneymap",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "postinstall": "cd moneymap-v2 && npm install",
    "dev": "cd moneymap-v2 && npm run dev",
    "build": "cd moneymap-v2 && npm run build",
    "start": "cd moneymap-v2 && npm run start",
    "lint": "cd moneymap-v2 && npm run lint"
  }
}
